// Galería de fotos por curso. Poné las imágenes reales en la carpeta
// galeria/<curso>/ (ej. galeria/2ITA/) y agregá cada una acá con su ruta.
// El carrusel las muestra automáticamente, en loop, sin botones —
// el usuario no lo controla, solo se puede pausar pasando el mouse encima.

const galeriaDB = {
    "2ITA": [
        { ruta: "galeria/2ITA/1.jpg" },
        { ruta: "galeria/2ITA/2.jpg" },
    ]
};